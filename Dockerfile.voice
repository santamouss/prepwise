FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

ENV NODE_ENV=production
ENV OPENAI_VOICE_RELAY_PORT=8080
ENV VOICE_RELAY_PORT=8080

EXPOSE 8080

CMD ["npx", "tsx", "server/openai-voice-relay.ts"]
